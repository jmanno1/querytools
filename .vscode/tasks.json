{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "run Binary",
            "type": "shell",
            "command": "${workspaceFolder}/target/debug/searcher.exe",
            "args": ["${workspaceFolder}/query.toml"]
        }
    ]
}